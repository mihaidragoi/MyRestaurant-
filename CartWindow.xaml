<Window x:Class="RestaurantIncercareaDoua.View.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Your Cart" Height="450" Width="500"
        Background="Azure" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Coșul tău de cumpărături" FontSize="20" FontWeight="Bold" Foreground="DarkBlue" Margin="0,0,0,10" Grid.Row="0"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produs" Binding="{Binding product.Denumire}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Preț Unitar" Binding="{Binding product.Pret}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Cantitate" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" IsReadOnly="True" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Detalii Livrare" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Adresă:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
                <TextBox Text="{Binding UserAddress, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Background="#F5F5F5" Grid.Row="0" Grid.Column="1" Margin="0,5" Padding="2"/>

                <TextBlock Text="Telefon:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
                <TextBox Text="{Binding UserPhone, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Background="#F5F5F5" Grid.Row="1" Grid.Column="1" Margin="0,5" Padding="2"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding TotalPlata, StringFormat='Total de plată: {0} RON'}" FontSize="18" FontWeight="Bold" Foreground="Green" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>

            <Button Content="Resetează Coșul" Command="{Binding ResetCartCommand}" Grid.Column="1" Background="Crimson" Foreground="White" FontWeight="Bold" Padding="5,10" Margin="0,0,10,0" Cursor="Hand"/>

            <Button Content="Plasează Comanda" Command="{Binding PlaceOrderCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Grid.Column="2" Background="BurlyWood" Foreground="White" FontWeight="Bold" Padding="5,10" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>